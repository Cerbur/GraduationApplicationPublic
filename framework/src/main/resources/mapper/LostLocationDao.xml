<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--namespace对应mapper接口所在的位置-->
<mapper namespace="top.cerbur.graduation.framework.dao.LostLocationDao">
    <select id="getAllLocation" resultType="top.cerbur.graduation.framework.bo.LostLocationBO">
        SELECT id, name, description
        FROM lost_location_tab
    </select>
</mapper>
